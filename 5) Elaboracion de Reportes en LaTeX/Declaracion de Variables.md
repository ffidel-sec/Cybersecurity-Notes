```LaTeX
\newcommand{\logoPortada}{LogoFinal.png}
```

Desgloce
`\newcommand` --> Aqui se le dice a LaTeX que se va a declarar una variable, es como un `let` de JavaScript.
`{\logoPortada}` --> Este es el nombre de la variable
`{Images/LogoFinal.png}` --> Contenido de la variable 