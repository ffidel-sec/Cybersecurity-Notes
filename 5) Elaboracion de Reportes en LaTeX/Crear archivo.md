En primer lugar, los documentos latex (donde se escribe el codigo) usan la extension _.tex_.

Para ver en vivo el documento en vivo usamos el siguiente comando:

```bash
latexmk -pdf Documento.tex -pvc
```